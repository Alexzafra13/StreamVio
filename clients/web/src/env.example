# Ejemplo de archivo .env para el cliente de StreamVio
# Copia este archivo a .env y ajusta los valores según tu entorno

# URL base de la API (usar la ruta relativa /api para mismo servidor)
PUBLIC_API_URL=/api

# Entorno (development, production, test)
NODE_ENV=development

# Puerto para el servidor de desarrollo
PORT=45000

# Token secreto para desarrollo (solo para pruebas)
PUBLIC_AUTH_TOKEN=

# Tiempo de vida de la sesión en horas
PUBLIC_SESSION_DURATION=24